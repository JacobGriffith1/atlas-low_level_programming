#include <stdio.h>

/**
 * main - Print numbers from zero to nine
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char d;
		for (d = 0; d <= 9; d++)
		{
			putchar(d);
		}
			{
				putchar('\n');
			}
		return (0);
}
