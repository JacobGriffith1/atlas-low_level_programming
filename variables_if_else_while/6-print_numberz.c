#include <stdio.h>

/**
 * main - Prints numbers zero to nine
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int d;
		for (d = 0; d < 10; d++)
		{
			putchar(d);
		}
			{
				putchar('\n');
			}
		return (0);
}
